modLoader="javafml" #mandatory

loaderVersion="[47,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.

license="All rights reserved"

issueTrackerURL="https://github.com/VincentHuto/HutosLib/issues" #optional

[[mods]] #mandatory
modId="hutoslib" #mandatory

version="5.2.1" #mandatory
displayName="HutosLib" #mandatory

# A URL to query for updates for this mod. See the JSON update specification https://mcforge.readthedocs.io/en/latest/gettingstarted/autoupdate/
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional

displayURL="https://legacy.curseforge.com/minecraft/mc-mods/hutoslib" #optional
logoFile="logo.png" #optional

# A text field displayed in the mod UI
credits='''
Huto,blusunrize(fluid GUI),shadows_of_fire(data sync)
'''

# A text field displayed in the mod UI
authors="Huto" #optional

# The description text for the mod (multi line!) (#mandatory)
description='''
Just a simple library mod to contain my common code through my mods,
as well as a few small nifty features!
~Huto
'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.hutoslib]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[47,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.hutoslib]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.20,1.20.2)"
    ordering="NONE"
    side="BOTH"
